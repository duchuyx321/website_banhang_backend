import { Application } from 'express';
const routers = (app: Application) => {
    // router user
    // router admin
    // router auth
    // router public
    app.use('/');
};

export default routers;
